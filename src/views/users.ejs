
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Profile</title>
	<link type="text/css" href="/stylesheets/style.css" rel="stylesheet" />
</head>
<body>

<div class="header">
		<span id="back_wrapper">
			<a href="/profile" target="_self">
				<img src="/images/back.png" alt="Back" title="Back">
			</a>
		</span>
	<span id="title_wrapper">
			All users (<%= users.length %>)
		</span>
	<div id="profile">
		<a href='/logout' >
			Sign Out
			<img src="/images/exit.png" alt="" style="vertical-align:middle">
		</a>
		<a href='/help' id="profileItem">
			Help
		</a>
	</div>
</div>

<div class="main">
	<div class="flex">
        <% if(users.length === 0){%>

		<div id="noFolder">There are no users</div>

        <%}else{%>

        <%for(let u of users){%>
		<div class="item">
			<a href="/users/u<%=u._id%>" target="_self">
				<img src="/images/user.png" alt="<%=u.name%>" onerror="imgError(this)"> <%=u.name%>
			</a>
		</div>
        <%}%>
        <%}%>
	</div>



</div>
</body>
</html>
