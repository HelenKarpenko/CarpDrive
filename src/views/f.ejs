<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%= curr.name %></title>
	<link type="text/css" href="/stylesheets/style.css" rel="stylesheet">
	<script>
        function imgError(image) {
            image.onerror = "";
            image.src = "/images/folder.png";
            return true;
        }
	</script>
</head>
<body>

<div class="header">
		<span id="back_wrapper">
			<%if(curr.parent=="5a088662347adc2a45b446e6"){%>
			<a href="/folders" target="_self">
				<img src="/images/back.png" alt="Back" title="Back">
			</a>
			<%}else{%>
			<a href="/folders/f<%=curr.parent%>" target="_self">
				<img src="/images/back.png" alt="Back" title="Back">
			</a>
			<%}%>
		</span>
	<span id="title_wrapper">
			Current folder: <%= curr.name%> <%=(curr.id)%>
		</span>
</div>

<div class="main">
	<div id="search_add">
		<form action="/folders/f<%= curr.id %>/add/" method="get">
			<button id="add" type="submit">
			</button>
		</form>
	</div>

	<div class="flex">
        <% if(children.length === 0){%>
		There are no folders
        <%}else{%>
        <%for(let f of children){ %>
		<div class="item">
			<a href="/folders/f<%=f.id%>" target="_self">
				<img src="/res/images/<%=f.img%>" alt="<%=f.name%>" onerror="imgError(this)"> <%=f.name%>
			</a>
		</div>
        <%}%>
        <%}%>
	</div>
</div>

</body>
</html>


